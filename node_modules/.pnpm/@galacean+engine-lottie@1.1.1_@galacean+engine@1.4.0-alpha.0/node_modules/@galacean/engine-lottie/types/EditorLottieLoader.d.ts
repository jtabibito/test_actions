import { AssetPromise, LoadItem, Loader, ResourceManager } from "@galacean/engine";
import { LottieResource } from "./LottieResource";
/**
 * @internal
 */
export declare class EditorLottieLoader extends Loader<LottieResource> {
    load(item: LoadItem, resourceManager: ResourceManager): AssetPromise<LottieResource>;
}
