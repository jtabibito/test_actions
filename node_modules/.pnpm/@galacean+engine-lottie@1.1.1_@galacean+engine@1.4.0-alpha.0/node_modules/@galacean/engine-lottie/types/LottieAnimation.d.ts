import { Script, Layer } from "@galacean/engine";
import { LottieResource } from "./LottieResource";
export declare class LottieAnimation extends Script {
    private static _pivotVector;
    private static _tempRenderers;
    /** The number of units in world space that correspond to one pixel in the sprite. */
    /** Repeat times of the animation. */
    repeats: number;
    /** whether the animation loop or not. */
    isLooping: boolean;
    /** whether the animation play back and forth */
    isAlternate: boolean;
    /** The direction of animation, 1 means play for */
    direction: 1 | -1;
    speed: number;
    pixelsPerUnit: number;
    private _alpha;
    private _width;
    private _height;
    private _resource;
    private _priority;
    private _priorityDirty;
    private _layer;
    private _layerDirty;
    private _clip;
    private _autoPlay;
    private _clipEndCallbacks;
    private _isPlaying;
    private _curFrame;
    private _frame;
    private _root;
    private _elements;
    set resource(value: LottieResource);
    get resource(): LottieResource;
    set priority(value: number);
    get priority(): number;
    set layer(value: Layer);
    get layer(): Layer;
    /**
     * global alpha
     */
    set alpha(value: number);
    get alpha(): number;
    set autoPlay(value: boolean);
    get autoPlay(): boolean;
    get frame(): number;
    get isPlaying(): boolean;
    /**
     * Play the lottie animation
     */
    play(name?: string): Promise<any>;
    /**
     * Pause the lottie animation
     */
    pause(): void;
    stop(): void;
    private _setLayer;
    private _createLayerElements;
    private _findEntityInTree;
    private _createElements;
    private _updateElements;
    private _updateElement;
    private _resetElements;
    /**
     * @override
     */
    onUpdate(deltaTime: number): void;
    /**
     * is this time frame spill the range
     */
    private _spill;
    /**
     * @override
     * @param target
     */
    _cloneTo(target: any): void;
    private _destroy;
    onDestroy(): void;
}
