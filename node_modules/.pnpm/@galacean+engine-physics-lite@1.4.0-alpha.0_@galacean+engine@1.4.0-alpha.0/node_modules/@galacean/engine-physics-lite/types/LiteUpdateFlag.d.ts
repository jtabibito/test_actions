/**
 * Used to update tags.
 */
export declare class LiteUpdateFlag {
    private _flags;
    /** Flag. */
    flag: boolean;
    constructor(_flags?: LiteUpdateFlag[]);
    /**
     * Destroy.
     */
    destroy(): void;
}
