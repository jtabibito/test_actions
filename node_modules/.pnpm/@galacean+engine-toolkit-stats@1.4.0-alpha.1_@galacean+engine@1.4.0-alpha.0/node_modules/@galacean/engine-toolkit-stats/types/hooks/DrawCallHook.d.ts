/**
 * @class DrawCallHook
 */
export default class DrawCallHook {
    drawCall: number;
    triangles: number;
    lines: number;
    points: number;
    private hooked;
    private readonly realDrawElements;
    private readonly realDrawArrays;
    private readonly realDrawElementsInstanced;
    private readonly realDrawArraysInstanced;
    private readonly gl;
    constructor(gl: WebGLRenderingContext | WebGL2RenderingContext);
    private hasInstancedFunction;
    private hookedDrawElements;
    private hookedDrawArrays;
    private hookedDrawElementsInstanced;
    private hookedDrawArraysInstanced;
    private update;
    reset(): void;
    release(): void;
}
