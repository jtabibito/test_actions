import { Vector2 } from "@galacean/engine";
import { DashMaterial } from "./material/DashMaterial";
import { Line } from "./Line";
/**
 * Dash Line.
 */
export declare class DashLine extends Line {
    protected _material: DashMaterial;
    private _dash;
    /**
     * The dash sequence is a series of on/off lengths in points. e.g. [3, 1] would be 3pt long lines separated by 1pt spaces.
     */
    get dash(): Vector2;
    set dash(val: Vector2);
    constructor(entity: any);
    protected _generateData(): Promise<{
        vertices: Float32Array;
        indices: Uint16Array;
    }>;
    protected _initMaterial(): void;
    private _generateDashTexture;
}
