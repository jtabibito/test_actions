import { Camera, Script, Vector3 } from "@galacean/engine";
/** @internal */
export declare class EndScript extends Script {
    private static _vector;
    private _flipView;
    private _flipSpeed;
    private _progress;
    private _sceneCamera;
    private _sceneCameraEntity;
    private _backEntity;
    private _textRenderer;
    private _textColor;
    private _target;
    private _normalQuat;
    private _tempMat;
    private _targetMat;
    private _currentMat;
    private _startMat;
    private _tempVect;
    private _tempEyeVect;
    private _upVector;
    private _disabledCompArray;
    private AxisFactor;
    /**
     * @return scene camera
     */
    get camera(): Camera;
    set camera(camera: Camera);
    /**
     * target point for gizmo, default (0,0,0)
     * @return target point
     */
    get target(): Vector3;
    set target(value: Vector3);
    onAwake(): void;
    onPointerEnter(): void;
    onPointerExit(): void;
    onPointerClick(): void;
    onUpdate(deltaTime: number): void;
    private _getTargetMatrix;
    private _disableComponent;
    private _enableComponent;
}
