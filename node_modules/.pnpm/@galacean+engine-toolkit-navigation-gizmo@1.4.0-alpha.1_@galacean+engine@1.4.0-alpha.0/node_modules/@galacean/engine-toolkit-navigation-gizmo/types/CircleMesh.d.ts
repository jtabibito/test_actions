import { Vector3, Engine, ModelMesh } from "@galacean/engine";
/**
 * create a circle mesh
 * @param engine - Engine
 * @param radius - Circle radius
 * @param segments - Number of segments
 * @param center - Circle center
 * @param normal - Circle direction
 * @returns Circle model mesh
 */
export declare function createCircleMesh(engine: Engine, radius?: number, segments?: number, center?: Vector3, normal?: Vector3): ModelMesh;
