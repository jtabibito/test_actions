import { Camera, Entity, Layer, Script, Vector2, Vector3 } from "@galacean/engine";
export declare class NavigationGizmo extends Script {
    private _sceneCamera;
    private _gizmoLayer;
    private _gizmoCamera;
    private _gizmoEntity;
    private _utils;
    private _target;
    private _sphereScript;
    private _endScript;
    /**
     * @position - gizmo position, the left upper point of the gizmo area, default (0, 0).
     * Normalized expression, the upper left corner is (0, 0), and the lower right corner is (1, 1).
     */
    position: Vector2;
    /**
     * @size gizmo size, the length and width of the gizmo area, default (0.12,0.12).
     */
    size: Vector2;
    /** scene camera
     * @return current scene camera
     */
    get camera(): Camera;
    set camera(camera: Camera);
    /**
     * target point for gizmo, default (0,0,0)
     * @return target point
     */
    get target(): Vector3;
    set target(value: Vector3);
    /**
     * gizmo layer, default Layer30
     */
    get layer(): Layer;
    set layer(layer: Layer);
    /**
     * @return gizmo camera's priority, larger than any other camera in scene, default 100
     */
    get priority(): number;
    set priority(priority: number);
    constructor(entity: Entity);
    onUpdate(): void;
    private _createGizmo;
    private _createAxis;
    private _createEnd;
    private _setTarget;
}
