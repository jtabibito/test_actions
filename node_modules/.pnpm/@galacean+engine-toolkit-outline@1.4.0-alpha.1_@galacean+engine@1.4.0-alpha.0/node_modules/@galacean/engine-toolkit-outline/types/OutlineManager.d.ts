import { Camera, Color, Entity, Layer, Script } from "@galacean/engine";
/**
 * Show outline of entities.
 * @decorator `@dependentComponents(Camera)`
 */
export declare class OutlineManager extends Script {
    /** whether outline children of selected entities with subColor, default false */
    isChildrenIncluded: boolean;
    private static _traverseEntity;
    private static _outlineColorProp;
    private static _outlineTextureProp;
    private static _texSizeProp;
    private static _replaceColorProp;
    private _outlineMaterial;
    private _replaceShader;
    private _renderTarget;
    private _screenEntity;
    private _size;
    private _clearColor;
    private _replaceColor;
    private _outlineMainColor;
    private _outlineSubColor;
    private _layer;
    private _outlineEntities;
    private _subLineEntities;
    private _renderers;
    private _layerMap;
    private _cameraViewport;
    private _outLineViewport;
    /**
     * Outline layer, default Layer29.
     */
    set layer(value: Layer);
    get layer(): Layer;
    /** Outline main color. */
    get mainColor(): Color;
    set mainColor(value: Color);
    /** Outline sub color. */
    get subColor(): Color;
    set subColor(value: Color);
    /** Outline size.[1~6] */
    get size(): number;
    set size(value: number);
    constructor(entity: Entity);
    /**
     * Clear all entities you want to outline.
     */
    clear(): void;
    /**
     * Add the entity you want to outline.
     * @param entity - The entity you wanna add.
     */
    addEntity(entity: Entity): void;
    /**
     * Remove the entity you do not want to outline.
     * @param entity - The entity you wanna remove.
     */
    removeEntity(entity: Entity): void;
    onEndRender(camera: Camera): void;
    onDestroy(): void;
    private _renderEntity;
    private _calSublineEntites;
    private _checkFrameBufferSize;
}
