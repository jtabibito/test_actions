import { TokenType } from "../common/types";
export default class LexerUtils {
    static numRegex: RegExp;
    static letterRegex: RegExp;
    static alphaRegex: RegExp;
    static numOrLetterRegex: RegExp;
    static isNum(char: string): boolean;
    static isLetter(char: string): boolean;
    static isAlpha(char: string): boolean;
    static isNumOrLetter(char: string): boolean;
    static isKeyword(tt: TokenType): boolean;
}
