import { BaseMaterial, Color, Engine } from "@galacean/engine";
/**
 * plain color Material. don't effected by light and fog.
 */
export declare class PlainColorMaterial extends BaseMaterial {
    /**
     * Base color.
     */
    get baseColor(): Color;
    set baseColor(value: Color);
    /**
     * Create a plain color material instance.
     * @param engine - Engine to which the material belongs
     */
    constructor(engine: Engine);
    clone(): PlainColorMaterial;
}
