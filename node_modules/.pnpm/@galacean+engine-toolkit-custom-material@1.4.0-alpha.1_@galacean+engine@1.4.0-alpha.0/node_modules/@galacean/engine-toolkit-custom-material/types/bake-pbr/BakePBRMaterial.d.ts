import { Engine, PBRMaterial, Texture2D } from "@galacean/engine";
/**
 * Bake PBR Material.
 */
export declare class BakePBRMaterial extends PBRMaterial {
    private static _lightMapTextureProp;
    private static _lightMapIntensityProp;
    /**
     * Light map texture.
     */
    get lightmapTexture(): Texture2D;
    set lightmapTexture(value: Texture2D);
    /**
     * Light map intensity, default 1.0.
     */
    get lightmapIntensity(): number;
    set lightmapIntensity(value: number);
    /**
     * Create a pbr metallic-roughness workflow material instance.
     * @param engine - Engine to which the material belongs
     */
    constructor(engine: Engine);
    clone(): BakePBRMaterial;
}
