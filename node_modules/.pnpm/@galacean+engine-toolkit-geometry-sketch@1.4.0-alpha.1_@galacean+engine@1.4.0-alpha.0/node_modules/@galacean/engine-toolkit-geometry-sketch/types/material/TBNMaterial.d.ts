import { BaseMaterial, Color, Engine } from "@galacean/engine";
/**
 * Material for normal shading
 */
export declare class NormalMaterial extends BaseMaterial {
    /**
     * Base color.
     */
    get baseColor(): Color;
    set baseColor(value: Color);
    constructor(engine: Engine);
}
/**
 * Material for normal tangent
 */
export declare class TangentMaterial extends BaseMaterial {
    /**
     * Base color.
     */
    get baseColor(): Color;
    set baseColor(value: Color);
    constructor(engine: Engine);
}
/**
 * Material for normal bi-tangent
 */
export declare class BiTangentMaterial extends BaseMaterial {
    /**
     * Base color.
     */
    get baseColor(): Color;
    set baseColor(value: Color);
    constructor(engine: Engine);
}
