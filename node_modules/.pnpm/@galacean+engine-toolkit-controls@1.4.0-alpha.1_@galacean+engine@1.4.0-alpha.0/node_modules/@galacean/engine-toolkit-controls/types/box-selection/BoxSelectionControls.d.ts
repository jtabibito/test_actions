import { BoundingFrustum, Camera, Entity, Script, InputManager, Vector2 } from "@galacean/engine";
import { IBoxSelectionHelper } from "./types";
export declare class BoxSelectionControls extends Script {
    startPoint: Vector2;
    endPoint: Vector2;
    collection: Entity[];
    instances: any;
    deep: number;
    camera: Camera;
    selectChildren: false;
    input: InputManager;
    isDeep: boolean;
    helper: undefined | IBoxSelectionHelper;
    onAwake(): void;
    onUpdate(): void;
    select(startPoint?: Vector2, endPoint?: Vector2): Entity[];
    updateBoundingFrustum(startPoint: Vector2, endPoint: Vector2): void;
    searchChildInBoundingFrustum(frustum: BoundingFrustum, entity: Entity, isDeep?: boolean): Entity[];
}
