import { BaseMaterial, Engine, Vector2, Vector4 } from "@galacean/engine";
export declare class BoxSelectionSSMaterial extends BaseMaterial {
    private static _borderWidth;
    private static _minPoint;
    private static _maxPoint;
    private static _boxColor;
    private static _borderColor;
    constructor(engine: Engine);
    get minPoint(): Vector2;
    set minPoint(value: Vector2);
    get maxPoint(): Vector2;
    set maxPoint(value: Vector2);
    get boxColor(): Vector4;
    set boxColor(value: Vector4);
    get borderColor(): Vector4;
    set borderColor(value: Vector4);
    get borderWidth(): number;
    set borderWidth(value: number);
}
