import { Color, Entity, Script } from "@galacean/engine";
/**
 * Skeleton visualization.
 * @example
 * rootEntity.addComponent(SkeletonViewer);
 */
export declare class SkeletonViewer extends Script {
    /** Distance from connector to bone, [0~1]. */
    midStep: number;
    /** The scale of the linker. */
    midWidthScale: number;
    /** Ball size. */
    ballSize: number;
    /** Skeleton Decrease Factor. */
    scaleFactor: number;
    /** The min color.  */
    colorMin: Color;
    /** The max color. */
    colorMax: Color;
    private _debugMesh;
    private _material;
    constructor(entity: Entity);
    onDestroy(): void;
    onEnable(): void;
    onDisable(): void;
    private _createSpur;
    private _showSkeleton;
}
